---
# yaml-language-server: $schema=https://schema.blue-build.org/module-stage-list-v1.json

name: ublue-sway
description: This is a Sway Ublue OS image.

base-image: ghcr.io/wayblueorg/sway
image-version: 43

modules:
  - from-file: common-modules.yml

  - type: rpm-ostree
    install:
      - wofi

  - type: os-release
    properties:
      ID: blue_build
      NAME: Sway
      PRETTY_NAME: Fedora Atomic Sway
